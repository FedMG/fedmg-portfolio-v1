import React from 'react';

const About = ({}) => {
  return (
    <div id="About" style = {{
      height: '100%',
      width: '100%',
      minHeight: '300px',
    }}>
      <h2>
        <span>About</span> me
      </h2>
    </div>
  );
};

export default About;
