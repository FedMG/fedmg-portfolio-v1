import React from 'react';

const Contact = ({}) => {
  return (
    <div
      id="Contact"
      style={{
        height: '100%',
        width: '100%',
        minHeight: '300px',
      }}
    >
      <h2>
        <span>Contact</span> me
      </h2>
    </div>
  );
};

export default Contact;
