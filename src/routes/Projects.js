import React from 'react';

const Projects = ({}) => {
  return (
    <div id="Projects" style = {{
      height: '100%',
      width: '100%',
      minHeight: '300px',
    }}>
      <h2>
        My<span> projects</span>
      </h2>
    </div>
  );
};

export default Projects;
